import styled from '@emotion/styled'
import { css } from '@emotion/react'

const mainContainerStyle = css`
  max-width: 1000px;
  margin: auto;
`

export const MainContainerStyled = styled.div(mainContainerStyle)